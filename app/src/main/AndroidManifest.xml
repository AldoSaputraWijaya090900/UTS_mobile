<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="umn.ac.id">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:requestLegacyExternalStorage="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".ProfileActvity"
            android:parentActivityName=".AwalActivity"
            android:label="Profile Saya"
            >
        </activity>
        <activity android:name=".ProfilDua"
            android:parentActivityName=".Tunes"
            android:label="Profile Saya"
            >
        </activity>
        <activity android:name=".Tunes"
            android:parentActivityName=".MainActivity"
            android:label="List Lagu"
            >
        </activity>
        <activity android:name=".PlayerActivity"
            android:parentActivityName=".Tunes"
            android:label="Now Playing"
            >

        </activity>
        <activity android:name=".MainActivity"
            android:parentActivityName=".AwalActivity"
            android:label="Halaman Login"
            >
        </activity>
        <activity android:name=".AwalActivity"
            android:label=" ">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>